#!/bin/bash
# this script will run on startup.

# 1. get latest vanilla version

# 2. check if latest version is higher than the current version

# 3. if latest version > current version, replace the current server.

# 4. start the server